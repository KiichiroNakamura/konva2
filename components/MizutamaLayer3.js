import React from "react";
import Konva from "konva";
import { Stage, Layer, Image, Circle, Rect } from "react-konva";

export const MizutamaLayer3 = () => {
  const layerw = new Konva.Layer();

  return layerw;

  // return (
  //   <Layer>
  //     <Circle fill="black" x={250} y={0} width={40} height={40} />
  //     <Circle fill="black" x={280} y={100} width={40} height={40} />
  //     <Circle fill="black" x={300} y={150} width={40} height={40} />
  //     <Circle fill="black" x={320} y={200} width={40} height={40} />
  //     <Circle fill="black" x={25} y={250} width={40} height={40} />
  //   </Layer>
  // );
};
